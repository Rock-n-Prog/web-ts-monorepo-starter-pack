{
  "name": "treeview",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.9.5",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "format": "prettier --quiet \"**/*.{js,jsx,ts,tsx,md}\"",
    "format:fix": "prettier --quiet --write \"**/*.{js,jsx,ts,tsx,json,css,md,yml}\"",
    "lint": "pnpm run lint:root && turbo run lint",
    "lint:root": "eslint ./*.{js,jsx,ts,tsx}",
    "lint:fix": "pnpm run lint:root:fix && turbo run lint:fix",
    "lint:root:fix": "eslint ./*.{js,jsx,ts,tsx}",
    "lint:staged": "lint-staged",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1",
    "turbo": "^1.5.1"
  },
  "lint-staged": {
    "./*.{js,jsx,ts,tsx}": [
      "pnpm lint:root:fix"
    ],
    "apps/web/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix apps/web/**/*.{js,jsx,ts,tsx}"
    ],
    "apps/docs/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix apps/docs/**/*.{js,jsx,ts,tsx}"
    ],
    "packages/web-ui/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix packages/web-ui/**/*.{js,jsx,ts,tsx}"
    ],
    "*.{js,jsx,ts,tsx,json,css,md,yml}": [
      "pnpm format:fix"
    ]
  }
}
