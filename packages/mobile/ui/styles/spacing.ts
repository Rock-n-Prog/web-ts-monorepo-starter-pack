import { normalize } from './layout';

const spacing = {
  xxs: normalize(4),
  xs: normalize(8),
  s: normalize(12),
  m: normalize(16),
  l: normalize(24),
  xl: normalize(36),
};

export { spacing };
